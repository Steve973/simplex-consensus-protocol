package org.storck.simplex.networking.api.network;

/**
 * Indicates the different network events that can occur.
 */
public enum NetworkEvent {
    PEER_DISCONNECTED, PEER_CONNECTED, NETWORK_DOWN, NETWORK_RESTORED
}